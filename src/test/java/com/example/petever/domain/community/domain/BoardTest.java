package com.example.petever.domain.community.domain;

import static org.junit.jupiter.api.Assertions.*;

class BoardTest {

    // 게시물은 회원만 작성이 가능하다
    // 게시물은 게스트도 볼 수 있다
    // 게시물은 대표 이미지가 없으면 기본 이미지로 넣는다
    // 게시물은 아이디, 작성시간, 댓글, 조회수, 태그를 표시한다
    // 게시물 컨텐츠를 활용해 프리뷰를 만든다
}